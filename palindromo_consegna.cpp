/*
 * Questo programma verifica se una parola e' un palindromo
 * (si legge uguale da sinistra a destra e da destra a sinistra)
 * usare le seguenti funzioni: isPalindroma (da completare)
 * Completare la funzione isPalindroma e il main
 */


#include <iostream>
using namespace std;

//la funzione isPalindroma verifica se la stringa e' un palindromo
//suggerimento: usare un ciclo for con i che va da 0 a n/2
//confrontare s[i] con s[n - 1 - i]: se sono diversi, restituire false
//se il ciclo finisce senza trovare differenze, restituire true
bool isPalindroma(string s) {
    //completare la funzione
}

int main() {
    cout << "Questo programma verifica se una parola e' un palindromo" << endl << endl;

    //chiedere all'utente una parola e leggerla con cin

    //chiamare isPalindroma e stampare il risultato
    //suggerimento: usare un if/else per stampare un messaggio appropriato

    cout << endl;

    return 0;
}
