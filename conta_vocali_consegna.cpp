/*
 * Questo programma conta quante vocali ci sono in una parola
 * usare le seguenti funzioni: contaVocali (da completare)
 * Completare la funzione contaVocali e il main
 */


#include <iostream>
using namespace std;

//la funzione contaVocali conta quante vocali ci sono nella stringa
//suggerimento: usare un contatore inizializzato a 0
//scorrere la stringa con un ciclo for da 0 a s.length()
//se s[i] e' una vocale (a, e, i, o, u, maiuscole o minuscole), incrementare il contatore
//restituire il contatore
int contaVocali(string s) {
    //completare la funzione
}

int main() {
    cout << "Questo programma conta le vocali in una parola" << endl << endl;

    //chiedere all'utente una parola e leggerla con cin

    //chiamare contaVocali e stampare il risultato
    //suggerimento: stampare "La parola contiene X vocali"

    cout << endl;

    return 0;
}
